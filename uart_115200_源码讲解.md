# UART 115200 模块源码讲解（对应 `uart_115200.v`）

> 目标：让你能“对照源码逐段看懂”，并知道每个寄存器和状态机在干什么。

---

## 1. 这份代码做了什么

`uart_115200.v` 实现了一个标准 UART `8N1` 串口模块：

- 1 位起始位（start = `0`）
- 8 位数据位（LSB first）
- 无校验位（No parity）
- 1 位停止位（stop = `1`）

默认参数：

- `CLK_FREQ_HZ = 50_000_000`
- `BAUD_RATE = 115200`

文件里有 3 个模块：

1. `uart_8n1`：顶层封装（把收发拼起来）
2. `uart_tx`：发送模块
3. `uart_rx`：接收模块

---

## 2. 顶层 `uart_8n1` 怎么看

顶层本身几乎不做逻辑，只做两件事：

- 例化一个 `uart_tx`
- 例化一个 `uart_rx`

你看到的写法：

```verilog
uart_tx #(...参数...) u_uart_tx (...端口...)
uart_rx #(...参数...) u_uart_rx (...端口...)
```

含义是：

- `uart_tx`/`uart_rx`：模块类型名
- `u_uart_tx`/`u_uart_rx`：实例名
- `#(...)`：参数赋值
- `(...)`：端口连接

---

## 3. `uart_tx` 发送模块逐段解释

### 3.1 核心参数

```verilog
localparam integer BAUD_DIV = CLK_FREQ_HZ / BAUD_RATE;
localparam integer CNT_W    = clog2(BAUD_DIV);
```

- `BAUD_DIV`：发送 1 bit 需要多少个时钟周期  
  50MHz / 115200 ≈ 434（整数分频）
- `CNT_W`：计数器 `baud_cnt` 需要的位宽

### 3.2 核心寄存器

- `baud_cnt`：波特率计数器，数满 `BAUD_DIV-1` 表示“1bit 时间到了”
- `bit_cnt`：当前发送到帧内第几位（0~9）
- `tx_shift`：10 位移位寄存器，存整帧数据

### 3.3 串口空闲电平

```verilog
assign tx = tx_busy ? tx_shift[0] : 1'b1;
```

- 空闲时（`tx_busy=0`）线为高电平 `1`
- 发送时输出 `tx_shift[0]`，每 bit 周期移位一次

### 3.4 开始发送时做了什么

`tx_start` 拉高后，会装载一帧：

```verilog
tx_shift <= {1'b1, tx_data, 1'b0}; // stop + data + start
```

因为总是先发 `tx_shift[0]`，所以发出的顺序是：

1. start = `0`
2. data[0]...data[7]
3. stop = `1`

这正好是 UART 8N1。

### 3.5 位发送推进

- `baud_cnt` 数满一次 -> 发送进入下一位
- `bit_cnt` 从 0 计到 9
- `bit_cnt==9` 后发送结束，`tx_busy` 拉低，`tx_done` 给一个时钟脉冲

---

## 4. `uart_rx` 接收模块逐段解释

### 4.1 输入同步（防亚稳态）

```verilog
rx_sync0 <= rx;
rx_sync1 <= rx_sync0;
```

`rx` 是异步外部信号，先过两级触发器再参与状态机判断，更稳。

### 4.2 接收状态机

状态定义：

- `S_IDLE`：空闲，等起始位
- `S_START`：确认起始位（在半 bit 处再次确认）
- `S_DATA`：按 bit 周期采样 8 个数据位
- `S_STOP`：检查停止位并输出数据有效

### 4.3 为什么要半位确认起始位

检测到下降沿后，不立刻当真，而是等 `HALF_BAUD` 再看一次：

- 还是 `0`：确认为真实起始位
- 回到 `1`：毛刺，丢弃

这一步能明显降低误触发。

### 4.4 数据采样与拼字节

在 `S_DATA` 状态中每满 1 bit 周期采样一次：

```verilog
rx_shift[bit_cnt] <= rx_sync1;
```

因为 UART 是 LSB first，这样放到 `rx_shift[0..7]` 正好对齐。

### 4.5 停止位校验与输出

在 `S_STOP` 中检查停止位为 `1`，才认为一帧合法：

- `rx_data <= rx_shift`
- `rx_valid <= 1'b1`（一个时钟脉冲）

---

## 5. `clog2` 函数是干什么的

代码里在 `uart_tx` 和 `uart_rx` 各写了一份 `clog2`：

```verilog
function integer clog2;
```

作用：计算“表示某个数字所需的最小位宽”。

为什么写两份？

- Verilog 中模块内函数默认是模块私有作用域；
- `uart_tx` 里定义的函数，`uart_rx` 不能直接调用；
- 所以各自放一份。

---

## 6. 一次发送/接收完整走读（帮助你建立时序感）

以发送字节 `8'hA5` 为例：

1. `tx_start` 拉高 1 个周期，`tx_busy` 置 1
2. `tx_shift` 装载 `{1'b1, 8'hA5, 1'b0}`
3. 每经过 `BAUD_DIV` 个时钟，移位一次
4. `tx` 线上依次输出：`0` + `10100101(LSB first)` + `1`
5. 最后一位发完，`tx_done` 脉冲 1 个周期

接收端：

1. 在 `rx` 上检测到起始位低电平，进入 `S_START`
2. 半 bit 后复核仍为低，进入 `S_DATA`
3. 每 bit 周期采样一次，拼成 `rx_shift`
4. 停止位为高后，输出 `rx_data`，并给 `rx_valid` 脉冲

---

## 7. 你现在最该关注的 5 个信号

调试时优先盯这些信号最有效：

- 发送侧：`tx_start`、`tx_busy`、`tx_done`
- 接收侧：`rx_busy`、`rx_valid`

经验：

- `tx_start` 必须是脉冲，不要长时间一直拉高
- `tx_busy=1` 时不要重复发起新一帧
- 读取 `rx_data` 以 `rx_valid` 为准

---

## 8. 常见问题（你后续大概率会遇到）

### 问题 1：为什么不是精确 115200？

因为 `BAUD_DIV` 用了整数除法，会有微小误差。  
多数场景可工作；若追求更准，可做小数分频/NCO。

### 问题 2：为什么接收容易偶发错误？

常见原因：

- 时钟误差过大
- 线路噪声
- 没有做起始位中点确认（你这份代码已经做了）

### 问题 3：为什么空闲线是高电平？

UART 协议规定就是这样：空闲=1，起始位=0。

---

## 9. 你可以怎么继续练

建议按这个顺序：

1. 先写 `uart_tx` 回环小测试（TX 接 RX）
2. 在 testbench 里发几组固定字节（`55`、`AA`、`00`、`FF`）
3. 观察 `rx_valid` 脉冲和 `rx_data` 是否一致
4. 再接上板级 USB-UART 做实测

---

如果你愿意，我下一步可以直接给你补一份 `uart_tb.v`（仿真 testbench），并把关键波形检查点也写好。

